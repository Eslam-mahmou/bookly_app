import 'package:flutter/material.dart';

class CustomSingleIndecator extends StatelessWidget {
  const CustomSingleIndecator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
